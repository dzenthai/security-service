services:
  pg:
    container_name: postgresql
    image: postgres:latest
    environment:
      - POSTGRES_USER=user
      - POSTGRES_PASSWORD=pass
      - POSTGRES_DB=security_db
    ports:
      - '5432:5432'